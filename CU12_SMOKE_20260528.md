# Local cu12 GPU smoke result (2026-05-28)

This fork branch (`cu121-mmdet33-gpu-validation-may28`) was used by the parent
sandbox `mmopenlab_cu12_sandbox` to verify cu12 GPU operation on a local box.

| field | value |
| :-- | :-- |
| date (UTC) | 2026-05-28 03:36:14 |
| GPU | NVIDIA RTX 2000 Ada Generation (sm_89, compute capability 8.9) |
| Driver | 550.127.08 (CUDA 12.4 runtime) |
| torch | 2.1.0+cu121 |
| mmcv | 2.1.0 (cu121 wheel) |
| mmdet | 3.3.0 (PyPI sdist; pure-python, no native ext) |
| mmengine | 0.10.x (latest at install time) |
| CUDA op | `mmdet.structures.bbox.bbox_overlaps` |
| op output device | `cuda` |
| op output shape | `[2, 2]`, dtype `torch.float32` |
| result | **success** |

## Required change candidates surfaced (vs. fork main `mmdet 3.3.0`)

- `mmdet/__init__.py:8-9`
  - Current: `mmcv_minimum_version = '2.0.0rc4'`, `mmcv_maximum_version = '2.2.0'` (exclusive `<`).
  - Smoke: works with `mmcv 2.1.0`. No fork patch was needed.
  - Future: if `mmcv 2.2.0` is required, the guard must be relaxed to `<=2.2.0`
    or `<2.3.0`. See upstream `open-mmlab/mmdetection#11680` (merged on
    `dev-3.x`) and `#11764` (closed without merge).
- `mmdet/__init__.py:12-13`
  - Current: `mmengine 0.7.1 - <1.0.0`. mmengine 0.10.x is within bounds.
- `setup.py:222`
  - Current: `ext_modules = []`. mmdet is pure-Python; no native build is
    triggered by cu12 changes.

Evidence (parent repo):
- `06_execution_logs/smoke_mmdet_20260528.json`

Workdoc / acceptance (parent repo):
- `temp/workdoc_May28-2026_cu12_submodule_gpu_validation.md`
- `temp/acceptance_May28-2026_cu12_submodule_gpu_validation.md`
